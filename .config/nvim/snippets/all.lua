return {
    -- Ejemplo de funcion con trigger y auto snippet
	-- s(
	--     {trig = "tr", snippetType = "autosnippet"},
	--     fmt(
	--          [[\overline{<>}]],
	--          { i(1) },
	--          {  delimiters = "<>" }
	--        )
	--     ),
    -- s("trig", t("loaded!!"))
}

